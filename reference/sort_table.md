# Sort table with control variables last

Sorts the table so that control variables (marked with "Y") appear after
regular variables, ordered by how many models they appear in.

## Usage

``` r
sort_table(table)
```

## Arguments

- table:

  A data frame with regression results

## Value

A sorted data frame
